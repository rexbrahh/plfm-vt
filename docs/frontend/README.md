# Web terminal WASM v1 contract bundle

This bundle adds three docs:

- `06-wasm-component-contract.md` : the stable contract and required WASM exports
- `07-worker-message-schema.md` : the main thread <-> worker message schema
- `08-render-loop.md` : render loop and damage policy

These files are designed to drop into:
`docs/frontend/web-terminal/`

