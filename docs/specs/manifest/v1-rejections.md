Rejections (explicit)
The v1 manifest must reject:
unknown top-level keys
autoscaling fields
privileged host mounts
non-tcp protocols in ports
custom secrets mount paths
unsupported volume filesystems
